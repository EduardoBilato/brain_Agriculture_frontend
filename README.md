# Rural Producer API - frontend

Para executar o front, navegue para a pasta raiz do projeto e execute o comando:
```dash
docker-compose up -d
```
<p>
A aplicação estará disponível na porta 3000 neste [link](http://localhost:3000)